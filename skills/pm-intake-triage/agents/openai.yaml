interface:
  display_name: "PM Intake Triage"
  short_description: "Triage questions, bugs, issues, and initiatives"
  default_prompt: "Use $pm-intake-triage to classify and consolidate these incoming reports."

policy:
  allow_implicit_invocation: true
